---
const { links } = Astro.props
import Button from "./Button.astro";
---
<div class="not-prose">
  {!!links.length && (
    <ul class="flex -mx-4 my-4">
      {links.map(({copy, url})=> (
        <li>
          <Button {copy} {url} />
        </li>
      ))}
    </ul>
  )}
</div>